{
	"name": "dolphin",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"build": "if-env TYPE=backend && (cd backend && npm install) || (cd client && npm install)",
		"server": "cd backend && npm start",
		"client": "cd client && npm run dev"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"if-env": "^1.0.4"
	}
}
